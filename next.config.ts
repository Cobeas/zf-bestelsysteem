import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // allowedDevOrigins: ['*.localhost:3000, app.localhost:3000'],
  // experimental: {
  //   serverActions: {
  //     allowedOrigins: ['*.localhost:3000', 'app.localhost:3000'],
  //   }
  // }
};

export default nextConfig;
